# Product

## Register

product

## Users

在 Windows 10/11 上频繁使用多个本机 Steam 账号的玩家与工具型用户。他们需要在不保存凭据、不绕过 Steam 验证的前提下，快速识别、整理并切换 Steam 已记住的本地登录状态。

## Product Purpose

新增账号必须经由 Steam 官方客户端完成登录并保存本地凭证。应用账号列表只呈现可切换账号；暂时失去凭证的历史账号保持资料但不进入日常界面。

账号列表负责快速识别与切换，右侧详情抽屉集中展示 Steam 登录账号、本地资料、标签及平台关联。多标签筛选采用全部满足规则，适合精确缩小账号范围。

账号页以头像、昵称、可登录状态、平台关联与标签为主要识别信息。Steam ID 是内部稳定标识，默认不进入日常视图；完美世界、5E、FACEIT 等平台关联自然形成筛选维度，取代可见的手工分组。已有玩家快照时，平台徽标同时显示段位与分数，不额外增加卡片或网络请求。

已关联 5E 的账号可通过唯一玩家名称、主页 ID或完整主页链接定位玩家，并在详情抽屉查看统一格式的玩家快照：身份、最近比赛后 ELO、聚合战绩与近期比赛。只选择平台名称也可建立普通关联，但不触发玩家查询。已有平台关联必须可编辑；更换玩家身份时清除旧缓存。界面必须区分实时请求、15 分钟缓存、过期回退、部分数据和接口结构变化；缺失字段显示未知，不伪造数据。

完美平台使用账号现有的 17 位 SteamID64 自动匹配，无需用户填写平台 ID。配置 Access Token 后，打开账号详情时自动建立或补全关联，并读取无需私有 signer 的赛季记录段位与分数。UI 必须标注“赛季记录分数”，不得宣称实时分数，也不得将 `score` 与 `elo` 混算；未配置 Token 时不发起请求，只说明配置条件。

5E 查询依赖非公开网页接口。可选 Token 只进入 Windows 凭据管理器，禁止进入 SQLite、日志、缓存、导入导出和错误文本；匿名查询是默认可用路径，凭据过期只提示并降级，不自动删除。

本地头像同步必须离线、最小权限：只从 Steam 本地头像缓存复制到应用专属目录，界面只访问应用缓存。清理不可用资料只影响本应用数据，不改变 Steam 登录列表或 Steam 文件。

集中展示本机 Steam 账号及其备注、标签和第三方平台关联，安全地完成可审计、可恢复的本地账号切换。成功意味着用户始终清楚当前状态、目标账号、风险和恢复路径。

## Brand Personality

视觉使用明亮冷色作为有限强调，提供极光蓝、脉冲紫、薄荷青和冰川白四套主题。账号色只用于头像底色、选择标记和小面积强调，不染整行背景。

克制、可靠、高效。语气直接、谨慎，不夸大本地推断的确定性。

## Anti-references

避免霓虹电竞界面、玻璃拟态、装饰性动效、模板化卡片墙，以及任何暗示 Valve 或第三方平台官方背书的视觉与文案。

## Design Principles

- 状态先于装饰：账号可用性、当前账号与风险必须一眼可辨。
- 危险操作可逆：切换、删除、恢复和清理都提供明确确认与反馈。
- 熟悉胜过新奇：沿用桌面工具的标准导航、表单和对话框行为。
- 证据决定措辞：无法确认时显示推测或未知，不制造确定性。
- 密度服务效率：紧凑但不牺牲键盘操作、可读性和高 DPI 缩放。

## Accessibility & Inclusion

以 WCAG 2.2 AA 为验收基线，支持完整键盘操作、可见焦点、减少动态、非颜色状态表达，并验证 Windows 100% 至 200% 缩放。
