# Product

## Register

product

## Users

在 Windows 10/11 上频繁使用多个本机 Steam 账号的玩家与工具型用户。他们需要在不保存 Steam 认证凭据、不绕过 Steam 验证的前提下，快速识别、整理并切换 Steam 已记住的本地登录状态。

## Product Purpose

新增账号必须经由 Steam 官方客户端完成登录并保存本地凭证。应用账号列表只呈现可切换账号；暂时失去凭证的历史账号保持资料但不进入日常界面。

账号列表负责快速识别与切换，左侧只展示 Steam 头像和 Steam 昵称。完美与 5E 固定为紧凑平台卡片：未填写时以无文案的灰色状态保留入口，填写后高亮并只显示平台用户名；点击卡片进入独立编辑弹窗。详情抽屉集中展示 Steam 登录账号、本地资料与标签，平台区域为只读卡片，展开后才按需加载战绩。多标签筛选采用全部满足规则，适合精确缩小账号范围。

默认账号页采用用户自定义全局顺序；只有在完整列表且没有搜索、标签、收藏或平台筛选时允许拖拽整条账号，键盘可通过拖拽手柄调整相邻位置。顺序写入普通应用设置，旧记录、重复 ID 和已删除账号会被安全归一化，新账号追加到末尾。Steam ID 是内部稳定标识，默认不进入日常视图。

账号首页启动时先加载已缓存账号，再后台扫描本机 Steam 列表并补齐头像与头像框，不以加载骨架锁住界面；之后每 10 秒静默重新扫描本机已记住的 Steam 账号与当前状态。手动扫描只在按钮上显示进度，列表仍可操作。账号页与平台页统一使用 Pointer Events 拖拽，拖拽只能从可见把手开始，账号行和平台行本身只作为放置目标，释放后持久化新顺序。

账号页列表顶端提供持久化的「只切 Steam」开关，默认打开：打开时只切换 Steam，不启动第三方平台；关闭时，Steam 切完后启动该账号已关联且本机已配置的 5E 和完美平台。开关文案固定，点击切换开/关。已关联 5E 时通过本机 `ActiveUser` 精确确认目标登录账号，稳定后才启动或重启 5E；信号缺失时使用有上限的兼容等待，明确为其他账号时不启动 5E。完美平台在 Steam 就绪后启动或重启。切换弹窗按当前开关和实际关联列出将执行的步骤。不自动启动 CS2 或 TeamSpeak。界面不放置“不保存密码 / Token / Cookie”这类承诺文案。

已关联 5E 的账号通过唯一平台用户名定位玩家，并在详情抽屉的展开卡片中查看统一格式的玩家快照：身份、当前排名状态、聚合战绩与近期比赛。主列表只有筛选到 5E 时才展示分数或定级场次，默认从低到高；未定级作为最低组排在已定级之前，组内以上赛季最终分数排序，没有上赛季分数时最低。上赛季参考分始终不在列表展示。已有平台关联必须可编辑；更换玩家身份时清除旧缓存。界面必须区分实时请求、15 分钟缓存、过期回退、部分数据和接口结构变化；缺失字段显示未知，不伪造数据。

完美平台使用账号现有的 17 位 SteamID64 自动匹配，无需用户填写平台 ID。配置 Access Token 后，在独立平台编辑弹窗保存资料时查询并补全关联；未配置 Token 时只保存人工识别信息，不发起请求。完美平台暂不参与主列表分数排序。

5E 查询依赖非公开网页接口。可选 Token 只进入 Windows 凭据管理器，禁止进入 SQLite、日志、缓存、导入导出和错误文本；匿名查询是默认可用路径，凭据过期只提示并降级，不自动删除。

每个完美平台或 5E 关联可额外保存平台登录账号、明文密码和备注，用于用户自行复制并完成短信等二次验证；这些字段不参与自动登录。密码默认遮罩但不加密，并进入用户主动导出的明文软件备份。软件备份不包含平台查询 Token、Steam 密码或 Steam 本地登录凭据；从文件恢复前先在应用数据目录静默保存当前业务数据，再事务性覆盖。

平台资料写入与玩家查询是两个独立结果：点击保存后必须结束编辑，查询失败不得迫使用户继续修改用户名；已保存但查询异常的平台在账号列表中使用红色卡片提示，用户可稍后重新编辑。备份导出始终包含全部业务分类；恢复时允许独立选择账号与平台资料、CFG 方案与运行记录或软件设置。扫描、切号和打开 CS2 页时会自动采集各账号已运行过的 CS2 userdata 配置并留下记录，用户改过的方案不会被覆盖。账号类数据只按 SteamID64 恢复到本机当前可用账号，未匹配的备份账号及其平台资料不得进入切号列表。网吧或不带 Steam 凭证时，使用「外出资料」页与独立资料包查看、复制 Steam 登录名、5E/完美账号和 CFG；这些记录在官方登录并勾选记住我之前不能切号。

平台页统一承载 Steam、5E、完美世界竞技平台与 TeamSpeak 3 的检测、路径配置和启动入口，默认顺序为 Steam、5E、完美、TS3，并允许通过把手拖拽后持久保存。Steam 与 TeamSpeak 安装包从 `cdn.qrqto.club` 直接下载；平台页对比本机版本与 CDN 清单，过期时可一键更新。左侧栏底部展示当前版本和更新按钮；启动和每隔 6 小时静默检查更新，点更新后从同一 CDN 下载安装包并自动重启。设置页其次展示切换设置和统一备份恢复；平台查询 Token 默认折叠并放在页面底部。

头像同步采用两级来源：本机 Steam 头像缓存用于轻量离线扫描；启动与手动扫描可从公开 Steam 社区资料补充动态头像和头像框。所有媒体只写入应用专属缓存，静态 JPG/PNG 与 GIF、动态 WebP、APNG 均保持原始文件内容，动画不得被转码或压平；自动 10 秒扫描不访问网络。清理不可用资料只影响本应用数据，不改变 Steam 登录列表或 Steam 文件。

集中展示本机 Steam 账号及其备注、标签和第三方平台关联，安全地完成可审计、可恢复的本地账号切换。成功意味着用户始终清楚当前状态、目标账号、风险和恢复路径。

## Brand Personality

视觉使用明亮冷色作为有限强调，提供极光蓝、脉冲紫、薄荷青和冰川白四套主题。账号色只用于头像底色、选择标记和小面积强调，不染整行背景。

克制、可靠、高效。语气直接、谨慎，不夸大本地推断的确定性。

## Anti-references

避免霓虹电竞界面、玻璃拟态、装饰性动效、模板化卡片墙，以及任何暗示 Valve 或第三方平台官方背书的视觉与文案。

## Design Principles

- 状态先于装饰：账号可用性、当前账号与风险必须一眼可辨。
- 危险操作可逆：切换、删除、恢复和清理都提供明确确认与反馈。
- 熟悉胜过新奇：沿用桌面工具的标准导航、表单和对话框行为。
- 证据决定措辞：无法确认时显示推测或未知，不制造确定性。
- 密度服务效率：紧凑但不牺牲键盘操作、可读性和高 DPI 缩放。

## Accessibility & Inclusion

以 WCAG 2.2 AA 为验收基线，支持完整键盘操作、可见焦点、减少动态、非颜色状态表达，并验证 Windows 100% 至 200% 缩放。
